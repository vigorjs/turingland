import React, { useState } from "react";

function PropertyCreateModal({
    developers,
    areas,
    isOpenCreateModal,
    setIsOpenCreateModal,
}) {
    const [loading, setLoading] = useState(false);
    
    return <div>PropertyCreateModal</div>;
}

export default PropertyCreateModal;
